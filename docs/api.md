# API reference

!!! note "Coming soon"
    A reference for the REST API. Until then, every running instance serves
    its own interactive API docs at `/docs` (Swagger UI, no CDN needed) and
    the raw schema at `/openapi.json`.
